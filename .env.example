# 厕哪 — 环境变量配置示例
# 复制本文件为 .env（或直接设置系统环境变量），填入真实值后使用
# .env 文件已被 .gitignore 排除，不会提交到 GitHub

# === proxy 模式（当前启用）===
# 前端走 Pages Function 代理，AMAP_KEY 配置在 Cloudflare Pages 后台：
#   Settings → Environment variables → Production/Preview
# 本地构建（node build.js）不需要 AMAP_KEY 环境变量

# === direct 模式（切回时启用）===
# 本地开发时设置后构建：浏览器直连高德 API，Key 注入到前端
# Windows:  set AMAP_KEY=你的key && node build.js
# Mac/Linux: AMAP_KEY=你的key node build.js
AMAP_KEY=your_amap_key_here

# === Cloudflare Pages 部署配置 ===
# CLOUDFLARE_ACCOUNT_ID  — Cloudflare Account ID（Dashboard 右侧栏）
# CLOUDFLARE_API_TOKEN   — Cloudflare API Token（需 Pages Edit 权限）
# 两者配置在 GitHub 仓库 Secrets 中，由 Actions 工作流读取
# CLOUDFLARE_ACCOUNT_ID=your_account_id
# CLOUDFLARE_API_TOKEN=your_api_token
